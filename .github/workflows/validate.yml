name: 🔍 Validar Projeto Node.js

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout do repositório
        uses: actions/checkout@v4

      - name: 🟢 Instalar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"

      - name: 📦 Instalar dependências
        run: npm install

      - name: 🧪 Teste rápido do monitor
        run: node src/monitor.js || echo "Monitor finalizou (sem travar)."

      - name: 🧹 Validar estrutura
        run: |
          echo "Verificando estrutura..."
          test -f package.json
          test -d src
          test -d logs
          echo "Estrutura OK!"

      - name: ✔️ Finalizado
        run: echo "Validação completa com sucesso!"
